# Claude Code adapter

@AGENTS.md

`AGENTS.md` is the canonical shared repository instruction file. This adapter exists only so Claude Code discovers the same contract automatically.

Do not fork or duplicate project policy here. If Claude-specific behavior is ever required, keep it minimal and link back to the canonical owner.
