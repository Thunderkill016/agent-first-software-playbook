# Security

This repository is public.

Do not submit:

- credentials;
- API keys;
- provider identifiers that should remain private;
- production logs containing user data;
- private repository content;
- internal incident details that create unnecessary exposure.

Use synthetic examples when demonstrating security-sensitive workflows.
