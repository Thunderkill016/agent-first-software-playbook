# Gemini CLI adapter

@./AGENTS.md

`AGENTS.md` is the canonical shared repository instruction file. This adapter exists only so Gemini CLI loads the same project contract without duplicating policy.

If Gemini-specific configuration becomes necessary, keep it as a thin adapter and preserve `AGENTS.md` as the cross-agent authority.
